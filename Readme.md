### Запуск
Весь код лежит в файле rag_pipeline.ipynb.
Для запуска необходимо произвести следующие действия в терминале из корня проекта

1) python3 -m venv venv
2) source venv/bin/activate
3) pip3 install -r requirements.txt
4) jupyter-lab
